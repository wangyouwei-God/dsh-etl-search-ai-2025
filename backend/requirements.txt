# Dataset Search and Discovery Solution - Backend Dependencies
# University of Manchester - Research Software Engineering
# Python 3.11+

# Web Framework
fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic==2.5.3
pydantic-settings==2.1.0

# HTTP Client (for downloading remote metadata)
requests==2.31.0
httpx==0.28.1

# Retry Logic (for flaky remote catalogues)
tenacity==8.2.3

# XML Parsing (for ISO 19115/19139 metadata)
lxml==5.1.0

# Database
sqlalchemy==2.0.25
alembic==1.13.1

# Vector Database & Embeddings (for semantic search)
torch==2.2.2
# IMPORTANT: grpcio must be pinned to a version with pre-built wheels.
# Without this, pip may download grpcio>=1.76.0 which requires C++ compilation
# from source, taking 10-15 minutes. Version 1.62.3 has pre-built wheels for
# all major platforms (Windows, macOS, Linux) and Python versions.
grpcio==1.62.3
chromadb==1.4.0
sentence-transformers==5.2.0
numpy==1.26.3

# Data Validation
python-multipart==0.0.6

# Environment & Configuration
python-dotenv==1.0.0

# Utilities
python-dateutil==2.8.2

# Document Parsing (for RAG content extraction)
pypdf==4.0.1  # PDF text extraction
PyMuPDF==1.26.7  # fitz-based PDF text extraction
python-docx==1.1.0  # DOCX text extraction

# Logging
python-json-logger==2.0.7
